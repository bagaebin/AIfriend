# Requestments 1 [적용 완료]
1. img. portrait-image를 div. portrait에 여백 공간 없이 꽉 채워주세요.
2. 웹사이트 설정 및 인터페이스 언어를 영어로 설정해주세요.

# Requsetments 2
1. 초기화면 및 이미지 생성 실패 시 보이는 'default.svg'를 삭제하고 div. portrait(사용자 초상) 영역에는 대신 div.bot-preview와 구조가 동일하게 통일한다. 그 구조를 설명하자면 아래와 같다.
- 상단에는 프로필 사진(예: div.bot-avatar), 하단에는 (예: div.bot-label)을 배치한 프로필 영역이 초상 영역 중앙에 표시된다. 
- 초상 영역의 배경은 어둡게 dim된다.

2. text 채팅 생성 기능과 image 생성 기능을 분리합니다. 
즉, 채팅 - 이미지 생성이 한 사이클 내에 있어 텍스트 생성과 이미지 생성을 모두 동시에 기다려야 하는 기존 구조에서, 채팅 응답은 즉각적으로 반응하되 채팅을 기반으로 업데이트된 프롬프트를 반영한 이미지 생성을 분리해 진행합니다.
<목적>
이미지 생성 시간이 길어 텍스트 응답까지 함께 오래 기다려야 하는 Idle Time을 최소화합니다.
<세부 수정사항 - 목적>
- 채팅을 연달아 하더라도 가장 최신에 생성된 이미지만 보여줍니다. - 이를 통해 빠른 템포의 대화를 하더라도 이미지 노출이 꼬이거나 오류가 생기지 않도록 합니다.
- 모든 텍스트 input마다 이미지를 생성하지 않습니다. prompt가 유의미하게 변경되어 결과물이 크게 달라질 것으로 예상될 때 새로운 이미지 생성 요청을 진행합니다.
* 기존 'needNewImag' 값을 현재는 항상 True로 두고 있으나, 이를 위 수정사항을 위해 활용하거나 수정할 수 있을지 검토합니다.